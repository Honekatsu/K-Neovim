# ReyoKatsu's Neovim Configs

macOS/Linux
```Zsh
cd ~/.config
git clone --depth=1 git@github.com:Honekatsu/K-Neovim.git nvim
```

Windows
```Powershell
cd $HOME\AppData\Local
git clone --depth=1 git@github.com:Honekatsu/K-Neovim.git nvim
```

